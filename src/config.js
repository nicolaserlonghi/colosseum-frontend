const configuration = {
    /* Release version */
    ip: "ostuni.xyz/coco",
    port: "",
    ssl: true,
    // Set handshake to false if is not necessary
    handshake: {
        magic: "coco",
        version: 1
    }
    /**/
};

export default configuration;