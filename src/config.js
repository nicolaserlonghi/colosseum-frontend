const configuration = {
    /* Release version */
    ip: "ostuni.xyz/coco",
    port: "",
    ssl: true,
    // Set handshake to false if is not necessary
    handshake: {
        magic: "coco",
        version: 1
    }
    /**/

    /* Debug version /
    ip: "192.168.178.252",
    port: "8088",
    ssl: false,
    // Set handshake to false if is not necessary
    handshake: {
        magic: "coco",
        version: 1
    }
    /**/
};

export default configuration;